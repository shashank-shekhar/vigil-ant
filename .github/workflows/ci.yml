# Security: Do not switch to pull_request_target or consume PR-controlled strings
# (github.event.*, github.head_ref, PR title/body/branch names) in run: steps
# without review — that is the classic GitHub Actions script-injection sink.
name: CI

# Least-privilege default token: this workflow only tests and builds.
permissions:
  contents: read

on:
  push:
    branches: [main]
    paths-ignore:
      - '*.md'
      - 'LICENSE'
      - 'docs/**'
      - 'scripts/**'
      - 'appcast.xml'
      - '.github/workflows/release.yml'
  pull_request:
    branches: [main]
    paths-ignore:
      - '*.md'
      - 'LICENSE'
      - 'docs/**'
      - 'scripts/**'
      - 'appcast.xml'
      - '.github/workflows/release.yml'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test-packages:
    name: Test Swift Packages
    runs-on: macos-15
    strategy:
      matrix:
        package: [GitHubKit, CIStatusKit]
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5.0.1

      - name: Test ${{ matrix.package }}
        run: |
          cd Packages/${{ matrix.package }}
          swift test

  build-app:
    name: Build App
    runs-on: macos-15
    needs: test-packages
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5.0.1

      - name: Create Secrets.xcconfig
        env:
          GH_CLIENT_ID: ${{ secrets.GH_CLIENT_ID }}
          SPARKLE_PUBLIC_KEY: ${{ secrets.SPARKLE_PUBLIC_KEY }}
        run: |
          cat > Secrets.xcconfig << XCCONFIG
          GH_CLIENT_ID = ${GH_CLIENT_ID:-placeholder}
          SPARKLE_PUBLIC_KEY = ${SPARKLE_PUBLIC_KEY:-placeholder}
          XCCONFIG

      - name: Build
        run: |
          xcodebuild build \
            -project Vigilant/Vigilant.xcodeproj \
            -scheme Vigilant \
            -destination 'platform=macOS' \
            CODE_SIGNING_ALLOWED=NO
